# ⚠️ DEPRECADO — NO desplegar ni usar como frontend

Este directorio es un **backup** del frontend React (SPA Vite) que fue
reemplazado por el frontend estático Liquid Glass.

- **Frontend real**: repo `agrotech-client-frontend` (desplegado en Netlify).
- Este código es una copia vieja (registro/login/landing) y NO es la fuente de verdad.
- No apuntar ningún build, Dockerfile ni `netlify.toml` a este directorio.
- Se mantiene solo como referencia histórica. Para recuperar una versión
  anterior, ver también el historial de git.

Fecha de archivado: agosto de 2026.
